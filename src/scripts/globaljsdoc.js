/**
 * A whole number representing a currency value.
 *
 * @typedef {(number)} currency
 * @example $2.52 ==> 252
 * @example -$0.10 ==> 10
 */

/**
 * A hex, rgb(a), or other CSS notation for a color
 *
 * @typedef {(string)} color
 * @example '#ff0000' => red
 * @example '#ff0' => yellow
 * @example 'rgba(0, 255, 0, 0.8)' => slightly transparent green
 */
